{
  "name": "agent-remote",
  "version": "0.1.0",
  "private": true,
  "description": "Android remote control for the agent-code desktop app (send commands to Claude Code over the LAN).",
  "scripts": {
    "sync": "cap sync android",
    "add:android": "cap add android",
    "build:apk": "node scripts/build-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
