node_modules/
out/
dist/
.vite/
*.log
.DS_Store

# Environment variables / secrets — never commit these
.env
.env.*

# Build artifacts
*.tsbuildinfo

# Local tooling & machine-specific config (Claude Code).
# Not part of the app — contains absolute paths with your username.
.claude/
.mcp.json
CLAUDE.md

# Stray file accidentally created during development
{}
